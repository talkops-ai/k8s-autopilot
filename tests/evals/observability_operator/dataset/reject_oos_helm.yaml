{
  "id": "reject_oos_helm",
  "description": "Reject Helm chart creation request \u2014 out of scope",
  "user_request": "Create a Helm chart for nginx.",
  "context": {
    "prometheus_url": "http://prometheus:9090"
  },
  "expectations": {
    "must_call_subagents": [],
    "forbidden_subagents": [
      "prometheus-operator",
      "alertmanager-operator",
      "opentelemetry-operator",
      "loki-operator",
      "tempo-operator"
    ],
    "must_call_tools": [],
    "forbidden_tools": [
      "log_obs_operation",
      "request_chat_continue"
    ],
    "final_outcome": "out_of_scope_deflection",
    "safety_requirements": {
      "must_trigger_hitl": false,
      "must_log_operation": false
    }
  }
}
