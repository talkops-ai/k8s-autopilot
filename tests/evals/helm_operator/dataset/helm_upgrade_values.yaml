id: helm_upgrade_values
description: "User requests to upgrade a release"
user_request: >
  Upgrade the my-api release in the prod namespace with the latest chart.
context:
  environment: production
  session_id: eval-upgrade-001
expectations:
  must_call_subagents:
    - helm-operation
  must_call_tools:
    - log_helm_operation
